# Human-readable mod name, available for mcmod.info population.
modName = AE2 Probability Pattern

# Case-sensitive identifier string, used for automatic mixin JSON generation.
# Spec v2.0: statpatterns (assets/statpatterns, mixins.statpatterns.late.json).
modId = statpatterns

# Root package of the mod.
modGroup = com.zincglux.statpatterns

# Mod version for this build (1.0 release).
# Note: gtnhgradle derives the actual version from a git tag (e.g. v1.0.0); this
# field is kept as documentation of the intended release version.
modVersion = 1.0.0

# Whether to use modGroup as the maven publishing group.
useModGroupForPublishing = true

# Updates build.gradle / settings.gradle automatically when an update is available.
autoUpdateBuildScript = false

# Version of Minecraft to target
minecraftVersion = 1.7.10

# Version of Minecraft Forge to target
forgeVersion = 10.13.4.1614

# MCP channel + mappings version for the decomp workspace
channel = stable
mappingsVersion = 12

# Default username for testing
developmentEnvironmentUserName = Developer

# Modern Java syntax support.
# false: No modern syntax, Java 8 only (matches AE2 rv3, safest for this port)
enableModernJavaSyntax = false

# ---- local proxy (127.0.0.1:7890) ----
systemProp.http.proxyHost=127.0.0.1
systemProp.http.proxyPort=7890
systemProp.https.proxyHost=127.0.0.1
systemProp.https.proxyPort=7890

# gtnhgradle 2.0.x requires a JVM 25; point Gradle at the installed JDK 25.
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-25.0.4.7-hotspot

# ---- Mixin (UniMixins via gtnhgradle MixinModule) ----
# Enables UniMixins: adds io.github.legacymoddingmc:unimixins:<ver>:dev (includes GTNHMixins
# late-phase API) to compile/runtime and wires the mixin annotation processor.
usesMixins = true
# Sub-package under modGroup (com.zincglux.statpatterns) that holds all @Mixin classes.
# Generated config package becomes com.zincglux.statpatterns.mixin.
mixinsPackage = mixin
