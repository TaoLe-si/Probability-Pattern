/*
 * Add your dependencies here. Supported configurations:
 *  - api("g:n:v:c"): if you use the types from this dependency in the public API
 *  - implementation("g:n:v:c"): internal implementation details
 *  - compileOnly("g:n:v:c"): not needed at runtime
 *  - devOnlyNonPublishable("g:n:v:c"): compiletime + runtime for dev, not published
 *  - runtimeOnlyNonPublishable("g:n:v:c"): runtime for dev runs, not published
 *  - annotationProcessor("g:n:v:c")
 */

dependencies {
    // Applied Energistics 2 rv3 (1.7.10) - GTNH fork.
    // Use the DEV (deobfuscated, MCP-named) jar; the release jar references SRG
    // fields and fails in a deobfuscated dev runtime (NoSuchFieldError field_xxx).
    // Version matches the user's GTNH instance: rv3-beta-695-GTNH.
    devOnlyNonPublishable(files("libs/appliedenergistics2-rv3-beta-695-GTNH-dev.jar"))

    // AE2 (GTNH 695) dev deps (from its dependencies.gradle)
    devOnlyNonPublishable("com.github.GTNewHorizons:NotEnoughItems:2.7.91-GTNH:dev")
    devOnlyNonPublishable("com.github.GTNewHorizons:GTNHLib:0.6.40:dev")
}
